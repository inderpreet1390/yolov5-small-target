To use these weights with the YOLOv5 repository, follow these steps: 
1. just download the weights file and the YOLOv5 repository
3. In the YOLOv5 repository, use the detect.py with the arguments --weights 'path to your weights file' --source 'path to source directory'
